apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-east-1

managedNodeGroups:
  - name: expense
    instanceTypes: ["m7-flex.large", "c7-flex.large","t3.small","t2.micro"]
    desiredCapacity: 3
    spot: false    